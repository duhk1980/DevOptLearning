name: Self-Hosted Runner Test on Windows

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: [LT-DUHOAC] #self-hosted
    steps:
      - name: Run a command
        run: echo "Self-hosted runner is working!"
